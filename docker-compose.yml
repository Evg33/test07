---
version: '2'
services:

  compiler:
    build: compiler
    volumes:
      - war:/app

  web:
    build: tomcat
    depends_on:
      - compiler
    ports:
      - "80:8080"
    volumes:
      - war:/opt

volumes:
  war:
